Changes to first paragraph:
	discrete biological models
	add some sentences about capabilities / tell what results software gives
	add some sentences about different input options 
		- GINsim, PDS, probabilistic networks (separate buttons for each)

make clear that the user can upload a file for all 3 options

disable number of states per node and Boolean/polynomial options 
unless user clicks on PDS

tell user what the output will be

gray out statespace option unless small networks is selected